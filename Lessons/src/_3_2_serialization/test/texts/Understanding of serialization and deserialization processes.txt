    Serialization - process of translating data structures or object state into 
a format that can be stored (for example, in a file or memory buffer, or 
transmitted across a network connection link) and reconstructed later in the 
same or another computer environment.

    Deserialization - process of extracting/removing the state of an object from
a stream.